/** Shared panel surface used by Card, PageSection, and inset controls. */
export const panelClassName =
  "rounded-lg border border-border bg-surface text-text shadow-panel";

/** Nested panels inside a white section (subtle lift). */
export const nestedPanelClassName =
  "rounded-lg border border-border/80 bg-surface text-text shadow-panel-sm";

/** Segmented control / tab bar track on a white surface. */
export const controlTrackClassName =
  "rounded-md border border-border/80 bg-background-soft/40 p-1 shadow-panel-sm";
